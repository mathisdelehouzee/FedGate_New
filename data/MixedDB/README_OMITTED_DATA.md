Le contenu brut de `MixedDB` n'est pas copie ici.

Ce qui est conserve :
- la documentation du dataset ;
- les fichiers descriptifs utiles pour le papier.

Ce qui n'est pas conserve :
- les NIfTI MRI bruts ;
- les sous-dossiers volumineux `ADNI-skull/`, `OASIS-skull/`, `NACC-skull/`, etc.

Pour une relance complete des experiences MRI, il faut restaurer le `MixedDB` original.
